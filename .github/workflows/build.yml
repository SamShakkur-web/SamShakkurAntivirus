name: 🛡️ Build SamShakkur Antivirus

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    name: 🪟 Build Windows
    runs-on: windows-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 🔍 Vérifier la structure
      run: |
        echo "Structure des fichiers :"
        dir /b
        echo "Src :"
        dir /b src
        echo "Data :"
        if exist data dir /b data
        echo "Resources :"
        if exist resources dir /b resources
        
    - name: 📦 Créer fichiers manquants
      run: |
        # Créer les dossiers nécessaires
        if not exist data mkdir data
        if not exist resources mkdir resources
        
        # Créer users.db s'il n'existe pas
        if not exist data/users.db (
          python -c "import sqlite3; conn = sqlite3.connect('data/users.db'); conn.close(); print('✅ Database created')"
        )
        
        # Créer icon.ico s'il n'existe pas
        if not exist resources/icon.ico (
          python -c "
try:
    from PIL import Image, ImageDraw
    img = Image.new('RGB', (256, 256), '#3498db')
    draw = ImageDraw.Draw(img)
    draw.rectangle([64, 64, 192, 192], fill='#FFA500')
    draw.ellipse([80, 80, 176, 176], fill='#FFFFFF')
    img.save('resources/icon.ico', format='ICO')
    print('✅ Icon created')
except Exception as e:
    print('⚠️ Could not create icon:', e)
"
        )
        
    - name: 🐍 Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: 📦 Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install streamlit==1.28.0
        pip install flask==2.3.3
        pip install stripe==5.5.0
        pip install pycryptodome==3.19.0
        pip install psutil==5.9.5
        pip install pandas==2.0.3
        pip install numpy==1.24.3
        pip install requests==2.31.0
        pip install python-dotenv==1.0.0
        pip install cachetools==5.3.1
        pip install Pillow==10.0.0
        pip install pyinstaller==5.13.0
        
    - name: 🏗️ Build executable
      run: |
        echo "🔨 Building executable..."
        pyinstaller `
          --name="SamShakkurAntivirus" `
          --icon="resources/icon.ico" `
          --add-data="src;src" `
          --add-data="data;data" `
          --add-data="resources;resources" `
          --add-data=".env;." `
          --hidden-import="streamlit" `
          --hidden-import="flask" `
          --hidden-import="stripe" `
          --hidden-import="Crypto" `
          --hidden-import="psutil" `
          --hidden-import="pandas" `
          --hidden-import="numpy" `
          --hidden-import="requests" `
          --hidden-import="python_dotenv" `
          --hidden-import="cachetools" `
          --console `
          --onefile `
          main.py
          
        echo "✅ Build completed!"
        
    - name: 📦 Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: SamShakkurAntivirus-Windows
        path: dist/SamShakkurAntivirus.exe
	
    - name: Debug complet
      run: |
       echo "=== DEBUG ==="
       python --version
       pip list
       echo "Fichiers :"
       dir /b /s
       echo "=== FIN DEBUG ==="
